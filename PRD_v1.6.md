# [프로젝트명] 근골격계 질환 업무관련성 평가 프로그램 기능 명세서

## 1. 프로젝트 개요 (Overview)
* **작성일:** 2026. 01. 28
* **문서 버전:** v1.6 (코드 리뷰 기반 리팩토링 및 기능 개선)
* **앱 버전:** 1.6.0
* **변경 이력:**
  * v1.1 → v1.2: 상병 입력 구조 변경, 누락 항목 추가, Excel Export 명세 구체화, 입력 검증 추가, 임시저장 기능 추가
  * v1.2 → v1.3: 신체부담정도 로직 수정 (L-101, L-102 삭제), 누적 신체부담 조건 수정 (>=50%), 파일 import 기능 추가
  * v1.3 → v1.4: 직업환경의학 전문의로 용어 수정, Vercel 배포, 모바일 지원, Windows 7 호환성, 직종 Preset 기능 추가
  * **v1.4 → v1.6:**
    * 컴포넌트 모듈화 (App.jsx 865→430줄, 5개 탭 컴포넌트 분리)
    * 계산 로직 통합 (`computePatientCalc()` 유틸리티)
    * 데이터 팩토리 패턴 (`createJob`, `createDiagnosis`, `createPatient`)
    * ID 생성: `Date.now()+Math.random()` → `crypto.randomUUID()`
    * alert/confirm → Electron IPC `dialog.showMessageBox` 전환 (포커스 문제 해결)
    * 신체부담정도 용어 변경: 고/중상/중하/하 → 고도/중등도상/중등도하/경도
    * 낮음사유 다중선택 지원 (Radio → Checkbox)
    * 미리보기 영역 화면 비례 높이 및 사용자 크기 조절 지원
    * 신체부담정도 설명 문구 추가 (미리보기/엑셀 출력)
    * GPU 비활성화 Windows 7 한정 조건부 적용
    * 미사용 코드 삭제 (`platform.js`)
    * 상수 추출 (`LOW_REASON_OPTIONS`, `AUX_LABELS`)
    * 배치 내보내기 유효성 검사 (이름 없는 환자 스킵)
    * 입력 시 에러 자동 클리어
    * 자동 임시저장 (30초 간격) 및 재진입 시 복원 확인
    * 환자 검색/필터/정렬 기능 (이름·진단·직종 검색, 완료/미완료 필터, 4종 정렬)
    * 환자 다중 선택 및 선택 Excel 내보내기 (ZIP)
    * 불러오기 추가(append) 모드 지원
    * PDF 출력 강화 (특이사항, 종합소견, 복귀고려사항, 평가기관정보, 보조요인 포함)
    * 종합소견 완료/미완료 상태 표시 (`isAssessmentComplete`)
    * 평가기관 기본값 설정 (병원명, 진료과, 의사명)
    * UI 전역 tooltip(title) 추가
    * 선택 환자 일괄 삭제 기능 (F-607 확장)
    * 사용자 설정 기능: 테마(라이트/다크), 폰트 크기(3단계), 기본 병원명/진료과/의사명, 자동저장 간격 (`SettingsModal.jsx`, `DEFAULT_SETTINGS`)
    * 다크 모드 지원: CSS 변수 기반 테마 시스템, 14개 CSS 변수 정의, 인라인 스타일 변수 전환
    * Export 로직 분리: `exportHandlers.js` (`genReport`, `generateEMRData`, `exportExcelSingle/Batch/Selected`, `exportPDF`, `buildWorkbook`)
    * Save/Load 로직 분리: `storageHandlers.js` (`loadSavedItems`, `savePatientsData`, `deleteSavedItem`, `hasDuplicateName`)
    * 환자 필터/정렬 커스텀 훅 분리: `usePatientList.js`
    * 저장명 중복 시 덮어쓰기 확인 (동일 이름 교체, IPC 확인 대화상자)
* **프로젝트 목적:** 근로자의 신체부담 요인을 입력받아 업무관련성(%)을 구간값(최소~최대)으로 자동 계산하고, 최종 의학적 소견을 포함한 리포트 생성 및 엑셀 데이터 연동을 자동화함.
* **주요 타겟:** 직업환경의학 전문의, 업무적 질병 판정 관련 실무자
* **플랫폼:**
  * 웹 서비스: Vercel 배포 (PC/태블릿/모바일 반응형)
  * 데스크톱: Electron 패키지 (Windows 7 32bit 이상 호환)

---

## 2. 기술 스택 (Tech Stack) — 실제 구현 기준

### 2.1. 프론트엔드
| 항목 | 기술 | 버전 | 비고 |
|------|------|------|------|
| **UI 프레임워크** | React | 18.2.0 | 함수형 컴포넌트 + Hooks |
| **빌드 도구** | Vite | 5.0.0 | HMR, 빠른 빌드 |
| **상태 관리** | React useState/useMemo | - | 별도 라이브러리 미사용 |
| **스타일링** | 순수 CSS | - | `src/index.css`, 반응형 미디어 쿼리 |
| **컴포넌트 구조** | 모듈화 탭 컴포넌트 | - | 5개 탭 + 모달 + 프리셋 검색 |

### 2.2. 데이터 처리
| 항목 | 기술 | 버전 | 비고 |
|------|------|------|------|
| **엑셀 I/O** | SheetJS (xlsx) | 0.18.5 | 단건/배치 내보내기, 배치 가져오기 |
| **ZIP 압축** | JSZip | 3.10.1 | 배치 내보내기 시 ZIP 아카이브 |
| **PDF 생성** | html2pdf.js | 0.10.1 | 서식 포함 PDF 출력 |
| **날짜 처리** | 직접 구현 | - | `calculations.js` 내 날짜 계산 함수 |

### 2.3. 데스크톱 패키지
| 항목 | 기술 | 버전 | 비고 |
|------|------|------|------|
| **프레임워크** | Electron | 21.4.4 | Windows 7 호환 |
| **빌드** | Electron Builder | 24.9.1 | NSIS 인스톨러, ia32+x64 |
| **압축** | Terser | 5.46.0 | 프로덕션 코드 최소화 |

### 2.4. 배포
| 항목 | 내용 |
|------|------|
| **웹** | Vercel 자동 배포 (GitHub push 시) |
| **데스크톱** | NSIS 인스톨러 (32bit/64bit 별도 또는 통합 빌드) |
| **버전 관리** | Git + GitHub |

---

## 3. 아키텍처 및 파일 구조

```
src/
├── App.jsx                          # 메인 앱 (~320줄, 상태관리/라우팅)
├── index.css                        # 전역 스타일 (반응형, CSS 변수 테마 시스템)
├── main.jsx                         # React 엔트리포인트
├── components/
│   ├── BasicInfoTab.jsx             # 기본정보 탭 (인적사항, 특이사항, 평가기관)
│   ├── DiagnosisTab.jsx             # 상병 탭 (진단 추가/삭제)
│   ├── JobTab.jsx                   # 직업력 탭 (프리셋, 보조요인)
│   ├── AssessmentTab.jsx            # 종합소견 탭 (SideAssessment 서브컴포넌트)
│   ├── ResultPanel.jsx              # 결과 패널 (계산결과, 미리보기)
│   ├── PresetSearch.jsx             # 직종 프리셋 검색 UI
│   ├── BatchImportModal.jsx         # 배치 가져오기 모달
│   └── SettingsModal.jsx            # 사용자 설정 모달 (테마, 폰트, 기본값, 자동저장)
├── hooks/
│   ├── useJobPresets.js             # 프리셋 데이터 로딩 훅
│   └── usePatientList.js            # 환자 검색/필터/정렬 훅
└── utils/
    ├── data.js                      # 팩토리 함수, 상수 정의 (DEFAULT_SETTINGS, FONT_SIZE_MAP 포함)
    ├── calculations.js              # 모든 계산 로직 통합
    ├── exportHandlers.js            # Export 로직 (Excel/PDF/EMR 생성)
    └── storageHandlers.js           # Save/Load/Delete 로직
electron/
├── main.js                          # Electron 메인 프로세스
└── preload.js                       # contextBridge API 노출
public/
├── job-presets.json                 # 직종 프리셋 데이터 (100+개)
└── icon.ico                         # 앱 아이콘
```

### 핵심 설계 원칙
1. **contextIsolation: true** + `nodeIntegration: false` 보안 모델
2. **IPC 통신**: `ipcMain.handle` / `ipcRenderer.invoke` via `contextBridge.exposeInMainWorld`
3. **웹/Electron 호환**: `window.electron?.method` 폴백 패턴
4. **계산 캐싱**: `useMemo` + `computePatientCalc()` 통합 유틸리티
5. **데이터 생성**: 팩토리 패턴 (`createPatient`, `createDiagnosis`, `createJob`)
6. **ID 생성**: `crypto.randomUUID()` (충돌 방지)
7. **완료 판정**: `isAssessmentComplete()` — 종합소견 입력 완료 여부 자동 판정 (상태 dot, 필터에 사용)
8. **테마 시스템**: CSS 변수(`--bg-panel`, `--text-primary` 등) + `[data-theme="dark"]` 다크 모드
9. **사용자 설정**: `localStorage` `wrEvaluationSettings` 키, `DEFAULT_SETTINGS` 상수 기반
10. **관심사 분리**: Export(`exportHandlers.js`), Storage(`storageHandlers.js`), 필터(`usePatientList.js`)를 App.jsx에서 분리

---

## 4. 상세 기능 명세 (Functional Requirements)

### 4.1. 입력 및 데이터 관리 (Input)

| ID | 카테고리 | 기능명 | 상세 설명 (로직 & UI) | 상태 | 데이터 타입/비고 |
| :-- | :-- | :-- | :-- | :-- | :-- |
| **F-101** | **기본정보** | **인적 사항 입력** | 1. 이름, 성별, 키, 몸무게, 생년월일, 재해일자 입력<br>2. **BMI 자동계산:** Weight / (Height(m)^2) — 소수점 1자리<br>3. **연령 자동계산:** (재해일자 - 생년월일) 만 나이 산출 | ✅ 구현 | `BasicInfoTab.jsx` |
| **F-105** | **기본정보** | **평가 기관 정보** | 1. 병원명 입력<br>2. 진료과 입력 (복수 가능)<br>3. 담당 의사명 입력 (복수 가능)<br>4. **평가 날짜:** 자동으로 현재 날짜 삽입 (수동 수정 가능) | ✅ 구현 | `BasicInfoTab.jsx` |
| **F-201** | **상병정보** | **상병 입력** | 1. **신청 상병:** 진단코드 + 진단명 (N개 추가 가능)<br>2. **부위:** 우측 / 좌측 / 양측 중 선택 (Radio Button)<br>3. 최소 1개 이상 진단 필수 (검증) | ✅ 구현 | `DiagnosisTab.jsx` |
| **F-202** | **상병정보** | **특이사항** | 1. **KLG 등급:** 우측(R) / 좌측(L) 별도 입력 (N/A, 1~4등급)<br>2. **기타 특이사항:** Textarea 제공 (산재이력, 상병상태 등) | ✅ 구현 | KLG: `AssessmentTab.jsx`<br>특이사항: `BasicInfoTab.jsx` |
| **F-301** | **직업력** | **직종 관리** | 1. **직종명 입력:** 프리셋 검색 또는 직접 입력<br>2. **Preset 선택 시:** 표준 신체부담 수치 자동 입력 (수정 가능)<br>3. 근무시작일, 종료일 입력 시 **근무기간** 자동 계산 (년/월 단위)<br>4. **근무기간 수동 입력:** "X년 Y개월" 형식 오버라이드 지원<br>5. 근거자료 Select (건강/연금, 고용/산재, 소득금액, 기타) — 복수 선택 가능<br>6. N개 직종 추가 가능<br>7. **실시간 신체부담 배지:** 색상 코딩된 등급 표시 | ✅ 구현 | `JobTab.jsx` + `PresetSearch.jsx` |
| **F-401** | **신체부담** | **핵심 노출요인** | 1. 쪼그려앉기/무릎꿇기 (분/일)<br>2. 중량물 취급량 (kg/일)<br>*직종(F-301)마다 개별 입력* | ✅ 구현 | `JobTab.jsx` |
| **F-402** | **신체부담** | **보조 노출요인** | 1. 6개 항목 체크박스:<br>   - 계단오르내리기, 무릎 비틀림, 출발/정지 반복<br>   - 좁은 공간, 무릎 접촉/충격, 뛰어내리기<br>2. 노출/비노출(T/F) 여부만 저장<br>*직종(F-301)마다 개별 입력* | ✅ 구현 | `JobTab.jsx`<br>상수: `AUX_LABELS` |

### 4.1-1. 직종 Preset 관리 (Job Preset)

| ID | 카테고리 | 기능명 | 상세 설명 (로직 & UI) | 상태 | 데이터 타입/비고 |
| :-- | :-- | :-- | :-- | :-- | :-- |
| **F-306** | **Preset** | **직종 Preset 라이브러리** | 1. **데이터 구조:** JSON 파일 형식<br>   - 직종명, 카테고리, 중량물 표준값, 쪼그려앉기 표준값, 설명, 출처<br>2. **내장 Preset:** 100+개 직종 (안산병원 자체협의 기준)<br>3. **폴백 프리셋:** 파일 로딩 실패 시 3개 기본 프리셋 내장<br>   - 건설 현장 배근공, 기계 조립원, 포장작업원<br>4. **확장성:** 관리자 Preset 추가/수정 가능 (Phase 3) | ✅ 구현 | `job-presets.json`<br>`useJobPresets.js`<br>버전 1.1.0 |
| **F-307** | **Preset** | **Preset 선택 UI** | 1. **검색형 드롭다운:** 실시간 필터링 (대소문자 무시)<br>   - 카테고리 \| 중량물 \| 쪼그려앉기 형식 표시<br>   - 최대 8개 결과 표시<br>2. **직접 입력 모드:** 프리셋 미선택 시 빈 입력란 활성화<br>3. **수정 가능성:** Preset 선택 후에도 수치 수정 가능 | ✅ 구현 | `PresetSearch.jsx` |
| **F-308** | **Preset** | **Preset 검색** | 1. 직종명 입력 시 자동완성 기능<br>2. 카테고리별 필터링<br>3. 키워드 검색 | ✅ 구현 | `PresetSearch.jsx` |

### 4.2. 종합 소견 및 평가 (Assessment)

| ID | 카테고리 | 기능명 | 상세 설명 (로직 & UI) | 상태 | 입/출력 |
| :-- | :-- | :-- | :-- | :-- | :-- |
| **F-501** | **종합소견** | **상병 확인** | 1. 신청 상병 불러오기<br>2. **확인 상병:** 진단코드 + 진단명 입력 (1개)<br>3. **부위별 평가:** 우측/좌측 각각에 대해<br>   - 확인 / 미확인 (Radio)<br>4. **참고:** KLG 등급 화면 표시 | ✅ 구현 | `AssessmentTab.jsx` |
| **F-502** | **종합소견** | **누적 신체부담** | 1. 업무관련성(Min, Max) 평균값 계산<br>2. **평균 >= 50%**: '충분함' 자동 표시<br>3. **평균 < 50%**: '불충분함' 자동 표시 | ✅ 구현 | `ResultPanel.jsx` |
| **F-503** | **종합소견** | **업무관련성 평가** | 1. 부위별(우측/좌측) Radio Button (높음 / 낮음)<br>2. 사용자가 판단하여 직접 선택 | ✅ 구현 | `AssessmentTab.jsx`<br>`SideAssessment` 서브컴포넌트 |
| **F-504** | **종합소견** | **평가 사유** | 1. 부위별로 "낮음" 선택 시 활성화<br>2. **5개 항목 다중선택** (체크박스):<br>   - 신체부담과 관련없는 상병<br>   - 상병 미확인 또는 연령대비 경미<br>   - 업무중단 후 상당기간 경과 진단<br>   - 누적 신체부담 낮음<br>   - 기타 (선택 시 텍스트 입력창 활성화)<br>*v1.4에서 4지선다 Radio → v1.6에서 5개 항목 다중선택 Checkbox로 변경* | ✅ 구현 | `LOW_REASON_OPTIONS`<br>`AssessmentTab.jsx` |
| **F-505** | **종합소견** | **복귀 관련 고려사항** | 1. Textarea 제공<br>2. 업무 복귀 시 고려사항 자유 입력 | ✅ 구현 | `AssessmentTab.jsx` |

### 4.3. 알고리즘 및 계산 (Logic)

| ID | 카테고리 | 기능명 | 상세 설명 (계산 공식) | 상태 | 입/출력 |
| :-- | :-- | :-- | :-- | :-- | :-- |
| **L-101** | **평가** | **직종별 신체부담정도** | 1. **각 직종별로** IFS 로직 적용하여 신체부담정도(고도~경도) 산출<br>2. 등급별 점수 매핑 (Min ~ Max)<br>3. 이 값들은 L-201 업무관련성 계산에 직접 사용됨 | ✅ 구현 | *하단 [로직 상세 1] 참조* |
| **L-201** | **결과** | **업무관련성(%)** | 1. 각 직종별 신체부담정도(L-101)를 공식에 대입<br>2. Min%, Max% 각각 계산<br>3. 화면 표시: **"Min% ~ Max%"** | ✅ 구현 | *하단 [로직 상세 2] 참조* |
| **L-301** | **내보내기** | **텍스트 병합** | 1. 모든 데이터와 계산 결과, 소견을 하나의 텍스트로 변환<br>2. 줄바꿈(\n) 활용하여 가독성 확보<br>3. EMR 필드 생성 (B5~B9 구조화 데이터) | ✅ 구현 | `exportHandlers.js`<br>`genReport()`<br>`generateEMRData()` |

### 4.4. 데이터 저장 및 불러오기 (Storage)

| ID | 카테고리 | 기능명 | 상세 설명 | 상태 | 비고 |
| :-- | :-- | :-- | :-- | :-- | :-- |
| **F-601** | **데이터관리** | **임시저장** | 1. "저장" 버튼 → 저장명 입력창<br>2. Local Storage에 저장 `{id, name, count, savedAt, patients}`<br>3. 여러 개의 평가 데이터 개별 저장 가능<br>4. 저장 시 타임스탬프 자동 기록<br>5. **중복 저장명 처리:** 같은 이름 존재 시 "이미 존재합니다. 덮어쓰시겠습니까?" IPC 확인 → 교체 | ✅ 구현 | `storageHandlers.js`<br>`wrEvaluationSavedItems` 키 |
| **F-602** | **데이터관리** | **불러오기** | 1. "불러오기" 버튼 → 저장된 데이터 목록 모달 표시<br>2. 목록: 저장명, 환자수, 저장 날짜/시간<br>3. **덮어쓰기 모드:** 기존 데이터 삭제 후 교체 (IPC 확인 대화상자)<br>4. **추가 모드:** 기존 데이터 유지 + 뒤에 추가 (새 ID 부여)<br>5. 삭제 기능 (IPC 확인 대화상자)<br>6. UI: 덮어쓰기/추가/삭제 3개 버튼 | ✅ 구현 | `dialog.showMessageBox` 사용 |
| **F-603** | **데이터관리** | **자동 임시저장** | 1. 30초 간격 자동 저장 (`wrEvaluationAutoSave` 키)<br>2. 재진입 시 "이전 자동 저장 데이터가 있습니다" IPC 확인 대화상자<br>3. 수동 저장 시 자동저장 데이터 클리어<br>4. 헤더에 마지막 자동 저장 시각 표시 (💾 아이콘) | ✅ 구현 | `App.jsx` |
| **F-604** | **데이터관리** | **파일 Import (Excel)** | 1. 드래그앤드롭 파일 업로드 (XLSX/XLS/CSV)<br>2. 파일 미리보기 테이블 (8열 × 5행)<br>3. **29개 필드 자동 인식:**<br>   - 기본(5): 이름, 생년월일, 재해일자, 키, 몸무게<br>   - 기관(3): 병원, 진료과, 의사<br>   - 부가(2): 특이사항, 복귀 고려사항<br>   - 진단(5): 코드, 명칭, 부위, KLG-R, KLG-L<br>   - 직업(7): 직종명, 시작일, 종료일, 기간-년, 기간-월, 중량물, 쪼그려앉기<br>   - 보조(6): 계단, 비틀림, 출발/정지, 좁은공간, 접촉, 뛰어내리기<br>4. **지능형 병합:**<br>   - 매칭키: 이름 + 생년월일 + 재해일자<br>   - 신규 환자 → 추가<br>   - 동일 환자 + 신규 진단 → 진단 추가<br>   - 동일 환자 + 신규 직업 → 직업 추가<br>   - 완전 중복 → 스킵<br>5. 가져오기 통계 표시 (신규/추가/스킵 건수) | ✅ 구현 | `BatchImportModal.jsx` |
| **F-605** | **데이터관리** | **파일 Import (CSV)** | F-604의 CSV 지원에 포함 (BatchImportModal에서 CSV도 처리) | ✅ 구현 | F-604에 통합 |
| **F-606** | **데이터관리** | **환자 검색/필터/정렬** | 1. **검색:** 사이드바 검색창에서 이름, 진단명, 직종명 실시간 필터링<br>2. **상태 필터:** 전체/완료/미완료 (종합소견 완료 기준, `isAssessmentComplete()`)<br>3. **정렬:** 입력순/이름순/생년월일순/평가일순<br>4. 검색/필터 시 "표시수/전체수" 카운트 표시 | ✅ 구현 | `usePatientList.js`<br>`calculations.js` |
| **F-607** | **데이터관리** | **환자 다중 선택** | 1. 환자별 체크박스 표시<br>2. 전체선택/해제 체크박스<br>3. 선택된 환자 수 표시<br>4. 선택된 환자 대상 Excel 내보내기 (O-102C)<br>5. **선택된 환자 일괄 삭제** (IPC 확인 대화상자, 최소 1명 유지 보호, 삭제 후 선택 초기화) | ✅ 구현 | `App.jsx` |
| **F-608** | **데이터관리** | **사용자 설정** | 1. 헤더 "⚙ 설정" 버튼 → 설정 모달<br>2. **테마:** 라이트/다크 전환 (CSS 변수 `data-theme` 속성)<br>3. **폰트 크기:** 작게(14px)/보통(16px)/크게(18px)<br>4. **새 환자 기본값:** 병원명, 진료과, 의사명 (새 환자 생성 시 적용)<br>5. **자동저장 간격:** 15초/30초/60초/사용안함<br>6. `localStorage` `wrEvaluationSettings` 키에 저장, 새로고침 시 복원 | ✅ 구현 | `SettingsModal.jsx`<br>`DEFAULT_SETTINGS` |

### 4.5. 입력 검증 (Validation)

| ID | 카테고리 | 기능명 | 상세 설명 | 상태 | 비고 |
| :-- | :-- | :-- | :-- | :-- | :-- |
| **V-101** | **검증** | **날짜 검증** | 1. 재해일자 >= 생년월일 확인<br>2. 근무종료일 >= 근무시작일 확인<br>3. 위반 시 에러 메시지 표시 | ✅ 구현 | |
| **V-102** | **검증** | **숫자 검증** | 1. 중량물 취급량 >= 0<br>2. 쪼그려앉기 시간 >= 0<br>3. 키, 몸무게 > 0 | ✅ 구현 | |
| **V-103** | **검증** | **필수 입력 필드** | 1. 필수 항목: 이름, 생년월일, 재해일자, 진단(≥1개, 코드+명칭), 직종(≥1개, 직종명)<br>2. 미입력 시 저장/출력 버튼 비활성화 또는 경고<br>3. **입력 시 에러 자동 클리어** | ✅ 구현 | `validate()` 함수 |

### 4.6. 출력 및 배포 (Output)

| ID | 카테고리 | 기능명 | 상세 설명 (Report & Export) | 상태 | 비고 |
| :-- | :-- | :-- | :-- | :-- | :-- |
| **O-101** | **리포트** | **화면 UI** | 1. 좌측: 입력 폼(탭 구조) / 우측: 실시간 결과 미리보기<br>2. 종합소견(F-501~505) 작성 폼 포함<br>3. **반응형 레이아웃:**<br>   - PC(≥900px): 2열 그리드<br>   - 태블릿(768~899px): 단일열, 사이드바 오버레이<br>   - 모바일(≤480px): 풀너비, 터치 최적화<br>4. **다중 환자 관리:** 좌측 사이드바에서 환자 목록 선택<br>5. **미리보기 영역:** 화면 비례 높이 + 사용자 크기 조절 가능 (resize)<br>6. **종합소견 완료 상태:** 환자별 완료/미완료 상태 dot 표시 (녹색/빨강)<br>7. **자동 저장 표시:** 헤더에 마지막 자동 저장 시각 표시<br>8. **UI 툴팁:** 주요 버튼/필드에 title 속성으로 설명 제공<br>9. **설정 버튼:** 헤더에 ⚙ 설정 버튼 (테마/폰트/기본값/자동저장 변경)<br>10. **다크 모드:** CSS 변수 기반 테마 시스템, 라이트/다크 전환 지원 | ✅ 구현 | `App.jsx` + 컴포넌트 |
| **O-102** | **내보내기** | **Excel Export (단건)** | 1. 2열 구조 XLSX 파일 생성<br>2. **EMR 필드:** B5(최종진단), B6(직업력), B7(개인요인), B8(종합소견), B9(복귀고려)<br>3. 파일명: `업무관련성평가_{이름}_{날짜}.xlsx`<br>4. 줄바꿈(\n) 활용하여 가독성 확보 | ✅ 구현 | SheetJS |
| **O-102B** | **내보내기** | **Excel Export (배치)** | 1. 전체 환자 일괄 내보내기<br>2. 환자별 개별 XLSX 파일 생성<br>3. ZIP 아카이브로 압축: `업무관련성평가_{N}명_{날짜}.zip`<br>4. 파일명: `{이름}_{재해일자}.xlsx`<br>5. **유효성 검사:** 이름 없는 환자 스킵 (스킵 수 알림) | ✅ 구현 | JSZip |
| **O-102C** | **내보내기** | **Excel Export (선택)** | 1. 체크박스로 선택된 환자만 일괄 내보내기 (F-607)<br>2. 환자별 개별 XLSX 파일 생성<br>3. ZIP 아카이브로 압축: `업무관련성평가_선택{N}명_{날짜}.zip`<br>4. 헤더에 "📊 Excel(선택 N)" 버튼 (선택 시에만 노출)<br>5. 이름 없는 환자 스킵 | ✅ 구현 | JSZip |
| **O-103** | **배포** | **패키지** | 1. Electron 기반 Windows Installer (NSIS, .exe)<br>2. **빌드 옵션:**<br>   - `electron:build` — ia32 + x64 통합 빌드<br>   - `electron:build:win32` — 32bit 전용<br>   - `electron:build:win64` — 64bit 전용<br>3. 한국어 언어 지원<br>4. 오프라인 실행 환경 보장 | ✅ 구현 | Electron Builder |
| **O-104** | **내보내기** | **PDF Export** | 1. html2pdf.js 라이브러리로 PDF 직접 생성<br>2. 서식 포함 테이블 레이아웃<br>3. Noto Sans KR 폰트, A4 용지 최적화<br>4. 색상 코딩된 신체부담 배지 포함<br>5. 파일명: `업무관련성평가_{이름}_{날짜}.pdf`<br>6. **포함 섹션:** 기본정보, 신청상병, 특이사항, 직업력(보조요인 포함), 신체부담기여도, 종합소견(상병별 좌우 평가·낮음사유), 복귀고려사항, 평가기관정보 | ✅ 구현 | html2pdf.js |

---

## 5. 로직 상세 (Logic Detail)

### [로직 상세 1] 직종별 신체부담정도 및 점수 매핑

#### 입력
* `W`: 중량물 취급량 (kg/일)
* `T`: 쪼그려앉기/무릎꿇기 (분/일)

#### 판정 로직

| 신체부담정도 | 설명 | 조건 | 점수 (Min ~ Max) |
|-------------|------|------|------------------|
| **고도** | 퇴행성 변화를 유발 또는 가속하는 것이 확실함(definite) | `(W >= 3000 AND T >= 180)` OR<br>`(W >= 3000 AND 120 <= T < 180)` OR<br>`(2000 <= W < 3000 AND T >= 180)` | **6.0 ~ 9.0** |
| **중등도상** | 퇴행성 변화를 유발 또는 가속하기에 충분함(probable) | `(W >= 3000 AND 60 <= T < 120)` OR<br>`(2000 <= W < 3000 AND 120 <= T < 180)` OR<br>`(W < 2000 AND T >= 120)` | **3.0 ~ 6.0** |
| **중등도하** | 퇴행성 변화를 유발 또는 가속할 가능성이 있음(possible) | `(W >= 3000 AND T < 60)` OR<br>`(2000 <= W < 3000 AND T < 120)` OR<br>`(W < 2000 AND 60 <= T < 120)` | **2.0 ~ 4.0** |
| **경도** | 퇴행성 변화를 유발 또는 가속하기 어려움(no related) | `(W < 2000 AND T < 60)` | **1.0 ~ 2.0** |

> **참고)** 미리보기 및 엑셀 출력에 위 4단계 설명이 자동 포함됨.

#### 예시
- 기계조립원 (W=1,500, T=120) → `(W < 2000 AND T >= 120)` → **중등도상** (3.0~6.0점)
- 포장작업원 (W=300, T=60) → `(W < 2000 AND 60 <= T < 120)` → **중등도하** (2.0~4.0점)
- 건설 배근공 (W=2,500, T=180) → `(2000 <= W < 3000 AND T >= 180)` → **고도** (6.0~9.0점)

---

### [로직 상세 2] 업무관련성 최종 공식

$$
\text{업무관련성(\%)} = \frac{\sum_{i=1}^{N} (\text{신체부담정도}_i - 1.0) \times \text{근무기간}_i}{1.0 \times (\text{연령} - 30) + \sum_{i=1}^{N} (\text{신체부담정도}_i - 1.0) \times \text{근무기간}_i} \times 100
$$

#### 변수 설명
* `N`: 전체 직종 수
* `신체부담정도_i`: i번째 직종의 신체부담정도 점수 (1.0 ~ 9.0) — L-101에서 산출
* `근무기간_i`: i번째 직종의 근무기간 (년 단위, 소수점 포함)
  * 수동 입력 오버라이드 지원 ("X년 Y개월" 형식)
* `연령`: 재해일 기준 만 나이

#### 특수 조건
* **연령 ≤ 30**: 결과 0% 반환 (의도적 설계)
* **직종 없음**: 결과 0% 반환

#### 계산 방식
1. 각 직종별 신체부담정도 산출 (L-101)
2. **최솟값 계산:** 각 직종의 최소 점수 대입
3. **최댓값 계산:** 각 직종의 최대 점수 대입
4. **결과 표시:** "XX.X% ~ YY.Y%" (소수점 첫째자리)

#### 계산 예시
**입력:** 연령 64세, 직종1(중등도상, 5.67년), 직종2(중등도하, 7.75년)

**최솟값:** `(3.0-1)×5.67 + (2.0-1)×7.75 = 19.09` → `19.09 / (34 + 19.09) × 100 = 36.0%`
**최댓값:** `(6.0-1)×5.67 + (4.0-1)×7.75 = 51.6` → `51.6 / (34 + 51.6) × 100 = 60.3%`
**결과:** "36.0% ~ 60.3%"

---

### [로직 상세 3] 누적 신체부담 자동 평가 (F-502)

```
평균관련성 = (업무관련성 최솟값 + 업무관련성 최댓값) / 2
IF 평균관련성 >= 50 THEN "충분함"
ELSE "불충분함"
```

---

## 6. 데이터 구조

### 6.1. 환자 객체 (`createPatient()`)
```javascript
{
  id: crypto.randomUUID(),
  data: {
    name: '',           // 이름
    gender: '',         // 'male' | 'female'
    height: '',         // cm
    weight: '',         // kg
    birthDate: '',      // YYYY-MM-DD
    injuryDate: '',     // YYYY-MM-DD
    hospitalName: '근로복지공단 안산병원',   // 병원명 (기본값)
    department: '직업환경의학과',           // 진료과 (기본값)
    doctorName: '김호길',                   // 의사명 (기본값)
    evaluationDate: '', // YYYY-MM-DD (오늘 날짜 자동)
    specialNotes: '',   // 특이사항
    diagnoses: [Diagnosis],
    jobs: [Job],
    returnConsiderations: ''  // 복귀 고려사항
  }
}
```

### 6.2. 진단 객체 (`createDiagnosis()`)
```javascript
{
  id: crypto.randomUUID(),
  code: '',              // ICD 코드
  name: '',              // 진단명
  side: '',              // 'right' | 'left' | 'both'
  confirmedCode: '',     // 확인 진단코드
  confirmedName: '',     // 확인 진단명
  klgRight: '',          // KLG 등급 (R)
  klgLeft: '',           // KLG 등급 (L)
  confirmedRight: '',    // '확인' | '미확인'
  confirmedLeft: '',     // '확인' | '미확인'
  assessmentRight: '',   // '높음' | '낮음'
  assessmentLeft: '',    // '높음' | '낮음'
  reasonRight: [],       // 낮음사유 다중선택 배열
  reasonRightOther: '',  // 기타 사유 텍스트
  reasonLeft: [],        // 낮음사유 다중선택 배열
  reasonLeftOther: ''    // 기타 사유 텍스트
}
```

### 6.3. 직업 객체 (`createJob()`)
```javascript
{
  id: crypto.randomUUID(),
  jobName: '',           // 직종명
  presetId: null,        // 프리셋 ID
  startDate: '',         // YYYY-MM-DD
  endDate: '',           // YYYY-MM-DD
  workPeriodOverride: '',// 수동 근무기간 ("X년 Y개월")
  evidenceSources: [],   // 근거자료 복수선택
  weight: '',            // 중량물 (kg/일)
  squatting: '',         // 쪼그려앉기 (분/일)
  stairs: false,         // 계단오르내리기
  kneeTwist: false,      // 무릎 비틀림
  startStop: false,      // 출발/정지 반복
  tightSpace: false,     // 좁은 공간
  kneeContact: false,    // 무릎 접촉/충격
  jumpDown: false        // 뛰어내리기
}
```

### 6.4. 주요 상수
```javascript
KLG_OPTIONS     // ['해당없음', '1등급', '2등급', '3등급', '4등급']
LOW_REASON_OPTIONS  // 5개 낮음사유 옵션 (다중선택)
AUX_LABELS      // 보조 노출요인 6개 레이블 매핑
FALLBACK_PRESETS // 기본 프리셋 3개 (파일 로딩 실패 시)
```

---

## 7. Electron 플랫폼 상세

### 7.1. 보안 모델
- `contextIsolation: true` — 렌더러/메인 프로세스 격리
- `nodeIntegration: false` — Node.js API 직접 접근 차단
- `contextBridge.exposeInMainWorld` — 안전한 API 노출

### 7.2. IPC API (`window.electron`)
| API | 설명 | 반환 |
|-----|------|------|
| `platform` | 운영체제 식별 | string |
| `onMenuNew(callback)` | 메뉴 "새로 만들기" 이벤트 | void |
| `showAlert(message)` | 네이티브 알림 대화상자 | Promise<void> |
| `showConfirm(message)` | 네이티브 확인 대화상자 | Promise<boolean> |
| `version` | 앱/Electron/Node/Chrome 버전 | object |

### 7.3. 메뉴 구조
- **파일:** 새로 만들기(Ctrl+N), 종료(Ctrl+Q)
- **편집:** 실행취소, 다시실행, 잘라내기, 복사, 붙여넣기
- **보기:** 새로고침(Ctrl+R), 전체화면(F11), 확대/축소
- **도움말:** 버전 정보 대화상자

### 7.4. 플랫폼별 처리
- **Windows 7 (NT 6.1):** `disable-gpu` 스위치 조건부 적용
- **Windows 10/11:** GPU 가속 유지
- **macOS:** 표준 앱 생명주기 처리

---

## 8. 반응형 UI 상세

### 브레이크포인트
| 구분 | 너비 | 레이아웃 |
|------|------|----------|
| Desktop (대) | ≥1400px | 2열 그리드, 최대 1800px |
| Desktop (소) | 900~1399px | 2열 그리드 |
| Tablet | 768~899px | 단일열, 사이드바 오버레이 |
| Mobile | ≤480px | 풀너비, 터치 최적화 |

### 색상 체계
| 용도 | 색상 |
|------|------|
| Primary | #667eea → #764ba2 (그라디언트) |
| 성공 | #51cf66 |
| 위험 | #ff6b6b |
| 정보 | #339af0 |
| 신체부담 고도 | 빨강 (#ffe0e0) |
| 신체부담 중등도상 | 노랑 (#fff3bf) |
| 신체부담 중등도하 | 녹색 (#d3f9d8) |
| 신체부담 경도 | 파랑 (#e7f5ff) |

---

## 9. 비기능 요구사항 (Non-Functional Requirements)

### 9.1. 데이터 보안
1. 서버 저장 없이 **Local Storage** 활용 (개인의료정보 보호)
2. 민감 정보 암호화 저장 고려 (선택)

### 9.2. 호환성
| 환경 | 상태 |
|------|------|
| Chrome 최신 (데스크톱) | ✅ 필수 |
| Edge 최신 | ✅ 권장 |
| Chrome (Android) | ✅ 필수 |
| Safari (iOS) | ✅ 필수 |
| Windows 7 SP1 32bit | ✅ 지원 (Electron) |
| Windows 10/11 64bit | ✅ 지원 |
| IE 11 | ❌ 미지원 |

### 9.3. 성능
1. 입력 폼 로딩: 2초 이내
2. 계산 및 미리보기 업데이트: 실시간 (1초 이내)
3. Excel Export: 5초 이내

---

## 10. 개발 우선순위 및 구현 상태

### Phase 1 (MVP) — ✅ 완료
- [x] F-101: 기본정보 입력
- [x] F-201, F-202: 상병정보 입력
- [x] F-301: 직종 관리
- [x] F-306, F-307: 직종 Preset 라이브러리 및 UI (100+개 직종)
- [x] F-401: 핵심 노출요인
- [x] L-101, L-201: 신체부담정도 평가 및 업무관련성 계산
- [x] F-501, F-502, F-503, F-504: 종합소견
- [x] O-101: 화면 UI (PC/태블릿/모바일 반응형)
- [x] O-102: Excel Export (단건)
- [x] V-101, V-102, V-103: 입력 검증
- [x] Vercel 웹 배포

### Phase 2 (개선 기능) — ✅ 완료
- [x] F-105: 평가 기관 정보
- [x] F-308: Preset 검색 및 자동완성
- [x] F-402: 보조 노출요인
- [x] F-505: 복귀 관련 고려사항
- [x] F-601, F-602: 임시저장 및 불러오기
- [x] F-604, F-605: 파일 Import (Excel, CSV)
- [x] O-104: PDF Export

### Phase 3 (확장 기능) — 대부분 구현
- [x] O-103: Electron 패키지 (Windows 7 32bit/64bit, Windows 10/11)
- [x] O-102B: 배치 Excel Export (ZIP)
- [x] O-102C: 선택 Excel Export (ZIP)
- [x] F-603: 자동 임시저장 (30초 간격)
- [x] F-606: 환자 검색/필터/정렬
- [x] F-607: 환자 다중 선택
- [ ] Preset 관리자 모드 (Preset 추가/수정/삭제)
- [x] F-608: 사용자 설정 (테마, 폰트 크기, 기본값, 자동저장 간격)
- [x] F-607 확장: 선택 환자 일괄 삭제
- [ ] 이력 관리 (동일 환자 이전 평가 기록)

---

## 11. 향후 개선 사항 (Future Enhancements)

1. **다국어 지원:** 영어, 중국어 등 추가 언어 지원
2. **클라우드 동기화:** 선택적 클라우드 백업 기능
3. **통계 대시보드:** 평가 건수, 업무관련성 분포 등 시각화
4. **템플릿 관리:** 여러 질병 유형별 템플릿 추가 (요추 질환, 어깨 질환 등)
5. **다른 부위 평가 프로그램 통합:**
   - MDDM을 이용한 허리(요추) 평가
   - HAL을 이용한 손목/팔꿈치 평가
   - 어깨 질환 평가 모듈
   - 통합 플랫폼: 하나의 프로그램에서 여러 부위 평가 수행
   - 부위별 결과 통합 리포트 생성
6. **AI 보조:** 과거 평가 데이터 기반 업무관련성 예측
7. **모바일 앱:** iOS/Android 네이티브 앱 개발
8. **협업 기능:** 여러 의사가 동시에 평가 작성 및 검토

---

## 12. 리포트 출력 구조

### 12.1. 미리보기 텍스트 (`genReport`)
```
업무관련성 특별진찰 소견서
[기본정보] 이름, 성별, 키/몸무게/BMI, 생년월일, 재해일자, 만 나이
[신청 상병] #1. 코드 진단명 (부위) ...
[특이사항] 자유 텍스트
[직업력]
  직력1: 직종명 | 근무기간 | 중량물 | 쪼그려앉기 | 신체부담정도
  보조: 해당하는 6개 항목
  (신체부담정도 4단계 설명 포함)
[신체부담기여도] min% ~ max%
[누적신체부담] 충분함/불충분함
[종합소견] 상병별 분석 (좌우 별도, 상태 및 업무관련성 평가)
[복귀 관련 고려사항]
[평가기관] 병원명, 진료과, 의사명, 날짜
```

### 12.2. EMR 데이터 구조 (`generateEMRData`)
| 필드 | 내용 |
|------|------|
| **B5** | 최종 확인 상병 + 부위별 평가 결과 |
| **B6** | 직업력 + 신체부담정도 + 기여도 |
| **B7** | 개인요인 (키, 몸무게, BMI, 나이, 특이사항) |
| **B8** | 종합소견 + 누적신체부담 + 업무관련성 평가 |
| **B9** | 복귀 관련 고려사항 |

---

**문서 끝 (End of Document)**
